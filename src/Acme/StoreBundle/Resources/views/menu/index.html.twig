{% extends 'AcmeStoreBundle::layout.html.twig' %}

{% block title %}MENU{% endblock %}

{% block content_header '' %}

{% block content %}	
    {% set version = constant('Symfony\\Component\\HttpKernel\\Kernel::MAJOR_VERSION') ~ '.' ~ constant('Symfony\\Component\\HttpKernel\\Kernel::MINOR_VERSION')%}

    <h1 class="title">Llibreria Menu Admin page</h1>
    
    	<div class="inline_menu" ><a href="{{ path('acme_llibre', {}) }}" >Llibres</a></div>
    	<div class="inline_menu" ><a href="{{ path('acme_noticia', {}) }}" >Noticies</a></div>
    	<div class="inline_menu" ><a href="{{ path('acme_agenda', {}) }}" >Agenda</a></div>
    	<div class="inline_menu" ><a href="{{ path('acme_presentacio', {}) }}" >Presentacio</a></div>
    	<div class="inline_menu" ><a href="{{ path('acme_galeria', {}) }}" >Galeria</a></div>
    	<div class="inline_menu" ><a href="{{ path('acme_encarrecs', {}) }}" >Encarrecs</a></div>
    	<div class="inline_menu" ><a href="{{ path('acme_pre_puja_txt', {}) }}" >Upload</a></div>
    	<div class="inline_menu" ><a href="{{ path('acme_slider', {}) }}" >Slider</a></div>
        {% if app.environment == 'dev' %}
           DEV
        {% endif %}
       
{% endblock %}