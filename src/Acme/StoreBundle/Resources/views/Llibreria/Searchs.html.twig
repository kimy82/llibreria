{% extends 'AcmeStoreBundle::layoutFrontend.html.twig' %}

{% block title %}Llibreria 22@{% endblock %}

{% block content_header '' %}

{% block content %}
    {% set version = constant('Symfony\\Component\\HttpKernel\\Kernel::MAJOR_VERSION') ~ '.' ~ constant('Symfony\\Component\\HttpKernel\\Kernel::MINOR_VERSION')%}

    <h1 class="title"></h1>
    
    	<!--{% for locale in ['en', 'fr', 'es'] %}
		    <li {% if locale == app.request.locale %}class="active"{% endif %}>
		        <a href="{{ path(app.request.get('_route'), app.request.get('_route_params')|merge({'_locale' : locale})) }}">{{ locale }}</a>
		    </li>
		{% endfor %}-->
		
    	
    	<div class="rightdiv">
    		
    	   {% if pagination|length >0 %}
	    		<ul>	    			
	    		{% for search in pagination %}
	    			{% if search.category == 'llibre' %}
	    				Llibre
	    			{% endif %}
	    			{% if search.category == 'noticia' %}
	    				Noticia
	    			{% endif %}
	    			{% if search.category == 'presentacio' %}
	    				Presentacio
	    			{% endif %}
	    			<li>{{ search.titol}} {{ search.description}}  <img width="200px" src="{{search.path}}{{search.attachment}}" /></li> 
	    		{% endfor %}
	    		</ul>
	    		<div class="pagerfanta" >
	    			{{knp_pagination_render(pagination)}}
	    		</div>
	    	{% else %}
	    	 <p>- There are no things </p>
	    	{% endif %}
    	</div>
        {% if app.environment == 'dev' %}
           DEV
        {% endif %}
       <script>		
				document.getElementById("sesuatu").value=window.localStorage.getItem('sesuatu');			
				
		</script>
{% endblock %}
